# BreastCare AI ML Service

# Directory containing model.joblib, feature_stats.json and model_info.json
ARTIFACTS_DIR=artifacts

# Railway injects PORT automatically in production.
# Use this only when running locally if needed.
PORT=8000
